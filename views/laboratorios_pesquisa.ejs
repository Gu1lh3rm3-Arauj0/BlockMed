<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Instituições de Pesquisa</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <style>
        .accepted-list-container {
            border: 1px solid #ddd;
            padding: 15px;
            border-radius: 5px;
            margin-top: 30px;
        }
        /* Style for the Close Proposal button */
        .close-proposal-btn {
            position: absolute;
            top: 20px;
            right: 20px;
            font-size: 1.2em;
            padding: 10px 20px;
            background-color: #dc3545;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .close-proposal-btn:hover {
            background-color: #c82333;
        }
    </style>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="/index.js" type="module"></script>
    <script src="https://cdn.jsdelivr.net/gh/ethereum/web3.js@1.6.0/dist/web3.min.js"></script>

</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">Instituições de Pesquisa</h1>

        <p class="text-muted">Wallet Address: <span id="walletAddress"></span></p>
        
        <!-- Close Proposal Button -->
        <form action="/close-proposal" method="POST">
            <input type="hidden" name="response_proposal" value="1">
            <button type="submit" class="close-proposal-btn">Encerrar Proposta</button>
        </form>

        <button id="connectButton" style="margin-top: 20px;">Connect to MetaMask</button>

        <button id="disconnectButton" disabled style="margin-top: 20px;">Disconnect from MetaMask</button>


        <form action="/submit-exam" method="POST">

            <div class="form-group">
        
                <label for="Nome">Nome do Laboratório:</label>
        
                <input type="text" id="Nome" name="nome_lab" class="form-control" required>
        
            </div>
        
            <div class="form-group">
        
                <label for="quantidade">Quantidade:</label>
        
                <input type="number" id="quantidade" name="quantidade" class="form-control" required>
        
            </div>
        
            <div class="form-group">
        
                <label for="preco">Preço por teste (em wey):</label>
        
                <input type="number" id="preco" name="preco" class="form-control" required>
        
            </div>
        
            <div class="form-group">
        
                <label for="reason">Tipo de Exame:</label>
        
                <input type="text" id="reason" name="reason" class="form-control" required>
        
            </div>
        
            <input type="hidden" id="walletAddressInput" name="walletAddress" value="<%= walletAddress %>">
        
            <button type="submit" class="btn btn-primary btn-block">Enviar</button>
        
        </form>

        <div class="accepted-list-container">
            <h2>Pacientes que aceitaram a proposta</h2>
            <ul class="list-group">
                <% acceptedPatients.forEach(patient => { %>
                    <li class="list-group-item"><%= patient.name %> - Wallet ID: <%= patient.wallet %> - Phone: <%= patient.phone %></li>
                <% }); %>
            </ul>
        </div>
    </div>
</body>
</html>
